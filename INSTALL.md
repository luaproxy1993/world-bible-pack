# Install — world-bible-pack v1.0.0

```bash
npx skills add luaproxy1993/world-bible-pack -y
```

Also install the text bible:

```bash
npx skills add luaproxy1993/world-bible -y
```

Then `/world-bible-pack` in the agent.
